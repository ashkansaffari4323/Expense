function headers(token,url,json=false){const h={Authorization:`Bearer ${token}`,Accept:'application/json'};if(json)h['Content-Type']='application/json';if(process.env.APS_COST_REGION&&url.includes('/cost/v1/'))h.Region=process.env.APS_COST_REGION;return h}
async function req(method,token,url,payload){const r=await fetch(url,{method,headers:headers(token,url,payload!==undefined),body:payload===undefined?undefined:JSON.stringify(payload)}),text=await r.text();let body;try{body=text?JSON.parse(text):null}catch{body=text}if(!r.ok)throw Error(`APS ${method} ${url} failed (${r.status}): ${typeof body==='string'?body:JSON.stringify(body,null,2)}`);return body}
module.exports={get:(t,u)=>req('GET',t,u),post:(t,u,p)=>req('POST',t,u,p),patch:(t,u,p)=>req('PATCH',t,u,p)};
