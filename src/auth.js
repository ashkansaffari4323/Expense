const crypto=require('crypto');const BASE='https://developer.api.autodesk.com/authentication/v2',COOKIE='aps_auth';
function config(){const clientId=process.env.APS_CLIENT_ID,clientSecret=process.env.APS_CLIENT_SECRET,callbackUrl=process.env.APS_CALLBACK_URL,scopes=process.env.APS_SCOPES||'data:read data:write account:read';if(!clientId||!clientSecret||!callbackUrl)throw Error('Missing APS environment variables');return{clientId,clientSecret,callbackUrl,scopes}}
const secret=()=>process.env.SESSION_SECRET||'change-me';const sign=v=>crypto.createHmac('sha256',secret()).update(v).digest('base64url');
function makeState(){const n=crypto.randomBytes(16).toString('hex');return`${n}.${sign(n)}`}function validState(v){const[n,s]=String(v||'').split('.');if(!n||!s)return false;try{return crypto.timingSafeEqual(Buffer.from(s),Buffer.from(sign(n)))}catch{return false}}
function encrypt(v){const key=crypto.createHash('sha256').update(secret()).digest(),iv=crypto.randomBytes(12),c=crypto.createCipheriv('aes-256-gcm',key,iv),data=Buffer.concat([c.update(Buffer.from(JSON.stringify(v))),c.final()]);return Buffer.concat([iv,c.getAuthTag(),data]).toString('base64url')}
function decrypt(v){try{const raw=Buffer.from(v||'','base64url'),iv=raw.subarray(0,12),tag=raw.subarray(12,28),data=raw.subarray(28),key=crypto.createHash('sha256').update(secret()).digest(),d=crypto.createDecipheriv('aes-256-gcm',key,iv);d.setAuthTag(tag);return JSON.parse(Buffer.concat([d.update(data),d.final()]).toString())}catch{return null}}
function getCookie(req){const out={};for(const part of String(req.headers.cookie||'').split(';')){const i=part.indexOf('=');if(i>-1)out[part.slice(0,i).trim()]=decodeURIComponent(part.slice(i+1).trim())}return decrypt(out[COOKIE])}
function setCookie(res,v){const secure=String(process.env.APS_CALLBACK_URL||'').startsWith('https://')?'; Secure':'';res.setHeader('Set-Cookie',`${COOKIE}=${encodeURIComponent(encrypt(v))}; Path=/; HttpOnly; SameSite=Lax${secure}; Max-Age=604800`)}function clearCookie(res){res.setHeader('Set-Cookie',`${COOKIE}=; Path=/; HttpOnly; SameSite=Lax; Max-Age=0`)}
function loginUrl(state){const c=config();return`${BASE}/authorize?`+new URLSearchParams({response_type:'code',client_id:c.clientId,redirect_uri:c.callbackUrl,scope:c.scopes,state}).toString()}
async function token(body){const c=config(),r=await fetch(`${BASE}/token`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded',Authorization:'Basic '+Buffer.from(`${c.clientId}:${c.clientSecret}`).toString('base64')},body});if(!r.ok)throw Error(await r.text());return r.json()}
const exchange=code=>{const c=config();return token(new URLSearchParams({grant_type:'authorization_code',code,redirect_uri:c.callbackUrl}))};const refresh=rt=>{const c=config();return token(new URLSearchParams({grant_type:'refresh_token',refresh_token:rt,scope:c.scopes}))};
async function ensure(req,res,next){let a=getCookie(req);if(!a?.refresh_token)return res.status(401).json({error:'Not signed in'});if(Date.now()>a.expires_at-60000){try{const t=await refresh(a.refresh_token);a={access_token:t.access_token,refresh_token:t.refresh_token||a.refresh_token,expires_at:Date.now()+t.expires_in*1000};setCookie(res,a)}catch{clearCookie(res);return res.status(401).json({error:'Session expired'})}}req.aps=a;next()}
module.exports={makeState,validState,loginUrl,exchange,setCookie,clearCookie,getCookie,ensure};
