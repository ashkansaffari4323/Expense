# Multi Expense v42

- Supplier Company is optional.
- Optional Purchase Order Name column loads contract names from each selected project.
- Contract IDs are hidden and mapped internally.
- Selected contract is sent as `contractId` on the parent expense and line item.
- Existing Expense Name in the same project remains the only duplicate blocker.
