UI Invalid Preview v62

Replace the public folder and package.json in the project root.

Preview lists only:
- Invalid rows
- Ignored rows
- Existing duplicate Expense Names

Valid rows are hidden from the table and remain visible only in summary counts.
